<h1>Chatbotの回答</h1>

<p><strong>あなたの質問:</strong> <%= params[:question] %></p>
<p><strong>Chatbotの回答:</strong>
  <%= sanitize(simple_format(@answer), tags: %w(a p br), attributes: %w(href title target rel))
       .gsub('<a ', '<a target="_blank" rel="noopener noreferrer" ').html_safe %>
</p>

<% if @search_results.present? %>
  <h2>関連する公式サイト</h2>
  <ul>
    <% @search_results.each do |place_data| %>
      <li>
        <strong><%= place_data[:name] %></strong>
        <% if place_data[:results].present? %>
          <ul>
            <% place_data[:results].first(3).each do |result| %>
              <li>
                <a href="<%= result.link %>" target="_blank"><%= result.title %></a><br>
                <small><%= result.snippet %></small>
              </li>
            <% end %>
          </ul>
        <% else %>
          <p>公式サイトが見つかりませんでした。</p>
        <% end %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= link_to "別の質問をする", homes_ask_path %>
